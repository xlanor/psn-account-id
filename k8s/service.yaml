apiVersion: v1
kind: Service
metadata:
  name: psn-account-lookup-api
  labels:
    app: psn-account-lookup-api
spec:
  selector:
    app: psn-account-lookup-api
  ports:
    - name: http
      port: 80
      targetPort: http

